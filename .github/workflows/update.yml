name: Update Cask

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version (without v prefix)"
        required: true
      sha256:
        description: "SHA256 hash of the DMG"
        required: true

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Update cask
        run: |
          sed -i "s/version \".*\"/version \"${{ inputs.version }}\"/" Casks/laya-serve.rb
          sed -i "s/sha256 \".*\"/sha256 \"${{ inputs.sha256 }}\"/" Casks/laya-serve.rb

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/laya-serve.rb
          git diff --cached --quiet && exit 0
          git commit -m "Update laya-serve to ${{ inputs.version }}"
          git push
